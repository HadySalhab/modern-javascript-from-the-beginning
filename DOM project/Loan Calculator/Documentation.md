# Loan Calculator

## Main functions

```Javascript
document.getElementById(element-id) // returns the element that has its id equal to the one being passed

parseFloat(String) //parse the string to float number

Math.pow(base-value,exponent-value) //Math calculation for power (base-value to the power of exponent-value)

isFinite(number) // determines whether a number is a finite, legal number.

number.toFixed(#ofdecimal) //Convert a number into a string, keeping only # of decimals specified:

//var num = 5.56789;
//var n = num.toFixed(2); -> 5.57

element.insertBefore(newChild,refChild) // insert the newChild before the refChild

 setTimeout(anyFunction, 3000); //fire the function after the time specified has passed

```
